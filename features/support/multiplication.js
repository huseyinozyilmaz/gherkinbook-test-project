const {Given,When,Then} = require('cucumber')
const assert = require('assert')

When('I multiply {int} by {int}', function (int, int2) {
  return 'pending'
})
Then('the result should be {int}', function (int) {
  return 'pending'
})
